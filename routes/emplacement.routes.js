module.exports = (app) => {
    const emplacement = require('../controllers/emplacement.controller.js');

    
    //app.post('/emplacement', emplacement.create);

    //app.get('/emplacement', emplacement.all);

    //app.get('/emplacement/:annoId', emplacement.findOne)

    //app.put('/emplacement/:annoId', emplacement.update)

    //app.delete('/emplacement/:annoId', emplacement.delete)
}