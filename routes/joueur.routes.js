module.exports = (app) => {
    const joueur = require('../controllers/joueur.controller.js');

    
    //app.post('/joueur', joueur.create);

    //app.get('/joueur', joueur.all);

    //app.get('/joueur/:annoId', joueur.findOne)

    //app.put('/joueur/:annoId', joueur.update)

    //app.delete('/joueur/:annoId', joueur.delete)
}